import React from 'react';

function ExcelRendererOutTableItem({ item }) {
    
    /*return (
        item.map((elem, index) => {
            <td>
                {
                    elem.name
                }
            </td>
        })
    )*/
}

export default ExcelRendererOutTableItem;